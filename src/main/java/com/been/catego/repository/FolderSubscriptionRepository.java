package com.been.catego.repository;

import com.been.catego.domain.FolderSubscription;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FolderSubscriptionRepository extends JpaRepository<FolderSubscription, Long> {
}
